{
  "name": "aharoom-web-apps",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel start",
    "start:rms": "npm run prepare && lerna run --parallel start --scope=@aha/ui --scope=@aha/rms",
    "prepare": "rimraf ui/build && lerna run prepare",
    "build": "npm run prepare && lerna run --parallel build",
    "storybook": "lerna run storybook --scope=@aha/storybook --stream",
    "build-storybook": "build-storybook"
  },
  "devDependencies": {
    "babel-loader": "^8.0.6",
    "lerna": "^3.16.4",
    "rimraf": "^2.6.3"
  },
  "dependencies": {},
  "workspaces": [
    "./rms",
    "./ui",
    "./storybook",
    "./config"
  ]
}
